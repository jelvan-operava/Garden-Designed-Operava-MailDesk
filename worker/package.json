{
  "name": "operava-maildesk-worker",
  "private": true,
  "scripts": { "dev": "wrangler dev", "deploy": "wrangler deploy", "check": "node --check src/index.js" },
  "devDependencies": { "wrangler": "^4.0.0" }
}
